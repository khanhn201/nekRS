# @package _global_
profile: False
halo_test: True
verbose: False
seed: 12
#epochs: 100
phase1_steps: 100
phase2_steps: 0
phase3_steps: 0
lr_phase12: 0.0001
lr_phase23: 0.0001
backend: "nccl"
#lr_init: 0.0001
# momentum: 0.5
use_noise : True
num_threads: 0
logfreq: 1
ckptfreq: 5
batch_size: 1
val_batch_size: 1
fp16_allreduce: False
restart: False
master_addr: none
master_port: none

# Modeling task
model_task: "time independent" # {time_independent, time series}
use_residual: False

# arch properties -- large gnn 
hidden_channels: 32
n_mlp_hidden_layers: 5
n_messagePassing_layers: 4

# Number of training rollout steps
rollout_steps: 1

# Halo swap mode 
halo_swap_mode: none

# plotting the connecivity (can take some time)
plot_connectivity: False

hydra:
  job:
    chdir: true

    #defaults:
    #  - override hydra/hydra_logging: colorlog
    #  - override hydra/job_logging: colorlog

# path to original working directory
# hydra hijacks working directory by changing it to the new log directory
# so its useful to have this path as a special variable
# https://hydra.cc/docs/next/tutorials/basic/running_your_app/working_directory
work_dir: ${hydra:runtime.cwd}
# path to folder with data
data_dir: ${work_dir}/datasets/
# path to folder for checkpointing
ckpt_dir: ${work_dir}/ckpt/ 
# path to saved model directory
model_dir: ${work_dir}/saved_models/ 
# path to save profile data  
profile_dir: ${work_dir}/outputs/profiles/new_timers/ 


# specify path to gnn_outputs here (case directory)  
gnn_outputs_path: ${hydra:runtime.cwd}/gnn_outputs_poly_7/
traj_data_path: ${hydra:runtime.cwd}/traj_poly_1/





